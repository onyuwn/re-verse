<h1>Ur playlists, sir</h1>

<table>
  <tr>
    <th>Playlist</th>
    <th>Tracks</th>
  </tr>
  <% @playlists.each do |p| %>
  <tr>
    <td><%= link_to p.name.to_s, {:controller => 'playlists', :action => 'timeline', :playlist => p.name, :user => @user.to_hash} %></td>
    <td>
      <ul>
        <% p.tracks.each do |t| %>
          <li><%=t.name %></li>
        <% end %>
      </ul>
    </td>
  </tr>
  <% end %>
</table>
